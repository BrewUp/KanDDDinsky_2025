Step 1
attach miro image
Create a C# API infrastructure for the bounded context "Purchase" from the Miro diagram attached. Do not implement Commands, Events and the code related to the domain, just stick to the infrastructure exposing the API calls. Write architecture and unit tests where necessary.

Step 2
attach miro image
Create all the specification tests for the domain and the unit tests for the infrastructure of the bounded context Purchase from the Miro diagram attached. Do not write the code.

Step 3
attach miro image
Create all the commands, events, command handlers, event handlers for the bounded context Purchase from the Miro diagram attached. Write architecture and unit tests where necessary.

Step 4
Create Docker compose necessary to run the Purchase bounded context with its dependencies (e.g., database, message broker, etc.). Ensure that the services can communicate with each other as required.
